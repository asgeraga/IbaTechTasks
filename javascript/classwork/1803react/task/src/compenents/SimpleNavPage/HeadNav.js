import React from 'react';

const HeadNav = props =>{
    const imgLogo = "";

    const links = props.navItems.map((nI, ind) =>{
        if (nI.toLowerCase() === "home"){
            return <Link key={ind} to=
        }
    }
}